from django.shortcuts import render
from django.http import HttpResponse                                                                                                                          fr                      from django.shortcuts import render
# Create your views here.
def home(request):
    return HttpResponse("Hello, Good morning")
                                              